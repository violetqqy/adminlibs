import { FormBase, FormType } from './form-base';
export class FormTime extends FormBase {
    constructor(options) {
        super(options);
        this.controlType = FormType.time;
    }
}
//# sourceMappingURL=form-time.js.map